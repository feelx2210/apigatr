// Gate Access Configuration
// Change the password here to update site access
export const GATE_CONFIG = {
  password: 'gat0rn0tcr0c',
  sessionKey: 'apigatr_authenticated',
  // Optional: Set session expiry in hours (0 = no expiry)
  sessionExpiryHours: 0
} as const;
